// REMOVED — location state now lives in Redux
// (frontend/src/store/locationSlice.js).
// Use: `useSiteLocation()` from store/hooks.js + thunks initLocation /
// requestPreciseLocation / setManualLocation + action clearLocation.
// This file is intentionally left as a stub so the folder can be deleted;
// do not import from here.
throw new Error(
  "LocationContext was removed — migrate to the Redux location slice (store/locationSlice.js)."
);
